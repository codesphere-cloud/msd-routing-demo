schemaVersion: v0.2
prepare:
  steps:
    - command: echo prepare
test:
  steps: []
run:
  foo:
    steps:
      - command: npx --yes serve msd-routing-demo/foo-src
    plan: 8
    replicas: 1
    network:
      path: /foo
  bar:
    steps:
      - command: npx --yes serve msd-routing-demo/bar-src
    plan: 8
    replicas: 1
    network:
      path: /foo/bar
